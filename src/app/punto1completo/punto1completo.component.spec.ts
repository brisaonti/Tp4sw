/*import { ComponentFixture, TestBed } from '@angular/core/testing';

import { Punto1completoComponent } from './punto1completo.component';

describe('Punto1completoComponent', () => {
  let component: Punto1completoComponent;
  let fixture: ComponentFixture<Punto1completoComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [ Punto1completoComponent ]
    })
    .compileComponents();
  });

  beforeEach(() => {
    fixture = TestBed.createComponent(Punto1completoComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});*/
